# Portry command

```bash
# Add package
poetry install

# update package
poetry update

# update packages with name
poetry update package1 package2

# check current env
poetry env info

# Add package
poetry add <package_name>

# Add package with latest version
poetry add package_name@latest

# Add package with version
poetry add <package_name>==<version>
```
